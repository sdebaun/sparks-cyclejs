deployment:
  production:
    branch: master
    commands:
      - gulp deploy --domain sparks-production.surge.sh
  staging:
    branch: release
    commands:
      - gulp deploy --domain sparks-staging.surge.sh
  staging-test:
    branch: deployment-changes
    commands:
      - gulp deploy --domain sparks-staging.surge.sh
